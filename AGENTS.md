# Repo Instructions

This repository contains Python + Playwright automation scripts.

## Quick Commands

- Install: `python -m pip install -r requirements.txt`
- Basic checks:
  - `python -m py_compile ai_duel.py ai_duel_webui.py`
  - `python -m compileall -q .`

